<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= pageTitle %> | LXH Portfolio</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include("partials/navbar") %>

  <main class="page">
    <section class="contact-card admin-project-card" style="max-width: 420px; margin-top: 2.5rem;">
      <h2>Enter Password</h2>

      <% if (error) { %>
        <p class="form-error"><%= error %></p>
      <% } %>

      <form action="/admin/check-password" method="POST" class="admin-project-form">
        <input type="hidden" name="go" value="<%= go %>" />

        <div>
          <label>Password</label>
          <input type="password" name="password" required />
        </div>

        <button type="submit" class="btn-send" style="margin-top: 1.3rem;">
          Continue
        </button>
      </form>
    </section>
  </main>

  <%- include("partials/footer") %>
</body>
</html>
